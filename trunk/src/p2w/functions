
export P2WDIR=$(cat $ENVDIR/P2WDIR)
export PORTSDIR=$(cat $ENVDIR/PORTSDIR)
export PORTSDIR_PATH=$(cat $ENVDIR/PORTSDIR_PATH)
export PORTSDIR_VERSION=$(cat $ENVDIR/PORTSDIR_VERSION)
export BASE_INPUTDIR=$(cat $ENVDIR/BASE_INPUTDIR)
export BASE_OUTPUTDIR=$(cat $ENVDIR/BASE_OUTPUTDIR)
export BASE_ROOTPATH=$(cat $ENVDIR/BASE_ROOTPATH)
export TREE_ROOTPATH=$(cat $ENVDIR/TREE_ROOTPATH)
export TGZ=$(cat $ENVDIR/TGZ)

function blank {
	echo '' >> ${1}
}
