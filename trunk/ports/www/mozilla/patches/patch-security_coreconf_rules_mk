$OpenBSD$
--- security/coreconf/rules.mk.orig	2004-03-22 17:56:47.000000000 -0700
+++ security/coreconf/rules.mk	2004-10-03 05:27:25.000000000 -0600
@@ -348,7 +348,6 @@ ifdef XP_OS2_VACPP
 else
 	$(MKSHLIB) -o $@ $(OBJS) $(SUB_SHLOBJS) $(LD_LIBS) $(EXTRA_LIBS) $(EXTRA_SHARED_LIBS)
 endif
-	chmod +x $@
 ifeq ($(OS_TARGET),Darwin)
 ifdef MAPFILE
 	nmedit -s $(MAPFILE) $@
